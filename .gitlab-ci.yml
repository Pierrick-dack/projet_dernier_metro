# .gitlab-ci.yml - S10 SCANS SÉCURITÉ SIMPLIFIÉS
stages:
  - test
  - security
  - build

test:
  stage: test
  image: node:18-alpine
  before_script:
    - cd api
    - npm install
  script:
    - npm test
  only:
    - main
    - merge_requests

security:npm-audit:
  stage: security
  image: node:18-alpine
  script:
    - cd api && npm audit --audit-level=moderate || true
  allow_failure: true
  only:
    - main
    - merge_requests

build:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.9.0-debug
    entrypoint: [""]
  script:
    - /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "api/Dockerfile"
      --destination "${CI_REGISTRY_IMAGE}:latest"
      --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_SHORT_SHA}"
      --cache=true
  only:
    - main